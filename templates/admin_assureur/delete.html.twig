{% extends "_admin_template.html.twig" %}

{% block title %}
    Supprimer l'assureur {{ assureur.nom }}
{% endblock %}

{% block content %}
    <div class="section">
        <h1 class="title has-text-centered">Supprimer l'assureur : "{{ assureur.nom }}"</h1>

        <div class="columns">
            <div class="column"></div>
            <div class="column is-half">
                {{ form_start(deleteForm) }}

                {{ form_row(deleteForm.confirmation, {
                    label: 'Je confirme la suppression de l\'assureur "' ~ assureur.nom ~ '"'
                }) }}

                <button type="submit" class="button is-danger is-fullwidth">
                    Supprimer
                </button>

                {{ form_end(deleteForm)}}
            </div>
            <div class="column"></div>
        </div>

    </div>
{% endblock %}

